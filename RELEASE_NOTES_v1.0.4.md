# 表格文件处理工具 v1.0.4

本次发布重点是代码结构重构、预览交互修正，以及发布产物与仓库行为的一致性整理。

本次内容：
- 将原先集中在单文件中的处理流程拆分为导入、字段匹配、处理管线、导出写回等多个模块，便于后续维护和继续开发
- 将 UI 拆分为运行时、数据源、处理流程、对比预览、控制面板等多个模块，降低界面代码耦合度
- 保留 `spreadsheet_tool.processor` 和 `SpreadsheetApp` 的兼容入口，避免影响现有调用方式
- 修复修改对比预览的长文本提示仅显示截断内容的问题，鼠标悬停时可查看完整值
- 优化大数据量原始预览的渲染方式，改为分批灌入，降低主线程卡顿风险
- 优化修改对比文本的生成方式，减少逐单元格插入导致的界面阻塞
- 调整导出设置说明文案，明确哪些选项仅在新建工作表时生效，避免界面误导
- 调整“移除选中文件”文案与提示，使其与实际“移除选中项”的行为一致
- 忽略本机运行日志和本地专用启动器，避免无关环境文件进入版本库
- 补充并更新相关回归测试，覆盖预览文本与对比渲染的新边界
